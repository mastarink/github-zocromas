/* TODO rename to duf_optimpl_class_enum.def */
#ifdef ENUM_WRAP
#ifdef ENUM_WRAP_V
  ENUM_WRAP_V(BAD, -2				)
  ENUM_WRAP_V(ALL, -1				)
  ENUM_WRAP(ANY					)

  ENUM_WRAP(MIN					)

  ENUM_WRAP(NONE				)
  ENUM_WRAP(NO_HELP				)

  ENUM_WRAP(HELP				)
  ENUM_WRAP(SYSTEM				)
  ENUM_WRAP(CONTROL				)
  ENUM_WRAP(FS					)
  ENUM_WRAP(DB					)
  ENUM_WRAP(SCCB				)
  ENUM_WRAP(REFERENCE				)
  ENUM_WRAP(COLLECT				)
  ENUM_WRAP(SCAN				)
  ENUM_WRAP(FILTER				)
  ENUM_WRAP(UPDATE				)
  ENUM_WRAP(REQUEST				)
  ENUM_WRAP(PRINT				)

  ENUM_WRAP(TRACE				)
  ENUM_WRAP(DEBUG				)
  ENUM_WRAP(OBSOLETE				)
  ENUM_WRAP(NODESC				)
  ENUM_WRAP(OTHER				)

  ENUM_WRAP(MAX					)
#endif
#endif
/*
vi: ft=c
*/
