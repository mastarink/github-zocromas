#ifndef MAS_DUF_DIRENT_SCAN2_H
#  define MAS_DUF_DIRENT_SCAN2_H

#  if 0
int duf_scan_dirents2( duf_depthinfo_t * pdi, duf_scan_hook2_dirent_t scan_dirent_reg, duf_scan_hook2_dirent_t scan_dirent_dir );
#  else
int duf_scan_dirents2( duf_depthinfo_t * pdi, duf_scanner_t scanner_dirent_reg2, duf_scanner_t scanner_dirent_dir2 );
#  endif

#endif
