# echo  "${BASH_SOURCE[0]}: \${MISHDIRS[working-ws]}: '${MISHDIRS[working-ws]}'" >&2
echo  "$( basename "${BASH_SOURCE[0]}" ): \${MISHDIRS[working-ws]}: '${MISHDIRS[working-ws]}'" >&2
if [[ -f shn/libwork.bash ]] ; then
  export MSH_SHN_PROJECTS_DIR=${MISHDIRS[working-ws]}
  [[ $MSH_SHN_PROJECTS_DIR ]] && source shn/libwork.bash
fi

# vi: ft=sh
